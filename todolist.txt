POS System TODO
===============

DONE
- M1  DB connected; products.php returns 4 seeded products as JSON
- M2  fetch loop renders products (async/await, DOM build)
- M2.5 barcode flow in cashier-pos.js (addByBarcode + renderProduct + alert)
- M2.5B recall session: rebuilt script from memory, fixed deltas, scratch deleted
- ENV Apache alias /pos -> repo folder (http://localhost/pos/ serves the app)
- ENV dashboards converted to pages/*.php shells (guards left for user)

M3 — REAL LOGIN (in progress at #1)
- [ ] 1. Hash seed passwords (admin/manager/cashier) via one-off PHP script in api/, run it, DELETE it
- [ ] 2. Write api/login.php   (read JSON body, prepared stmt, password_verify, session_start)
- [ ] 3. Write api/logout.php  (session_unset + session_destroy, redirect to ../index.html)
- [ ] 4. index.html: remove the role dropdown (role comes from DB now)
- [ ] 5. Rewrite js/login.js: POST JSON to /pos/api/login.php, redirect to pages/<role>-dashboard.php
- [ ] 5b. cashier-pos.js line 12: fetch URL is now http://localhost/pos/api/products.php  (DONE once changed)
- [ ] 6. Add session guard + echo $_SESSION at the @M3 markers in all three pages/*.php
- [ ] 7. Delete stale pages/*.html (after login routes to .php work)  [AI does with user's OK]
- [ ] 8. Test admin/password123 -> Admin dashboard -> logout -> bounced to index.html
- [ ] 9. Teach-back: explain login.php + one guard line-by-line

DONE — M3 PORTABILITY
- [x] API moved INTO the repo (api/config.php, api/products.php) served at /pos/api/
- [x] Old C:\xampp\htdocs\pos_accounting removed (no double copies)
- [x] .gitignore added (rules: OS junk, .env, *.local.php, vendor)
- [x] Repo hygiene rule learned: git status + stage specific paths, never blind add .
- [ ] (optional later) Dockerize with volume-mount + database.sql init if teacher demo needs it

M4+ (later)
- Checkout/charge: subtotal->discount->tax->total, save transaction to DB
- Inventory restock, reports/dashboard stats, payroll (per the Excel checklist)