POS System TODO
===============

DONE
- M1  DB connected; api/products.php returns 4 seeded products as JSON
- M2  fetch loop renders products (async/await, DOM build)
- M2.5 barcode flow in cashier-pos.js (addByBarcode + renderProduct + alert)
- M2.5B recall session: rebuilt script from memory, fixed deltas, scratch deleted
- ENV Apache alias /pos -> repo folder; dashboards converted to pages/*.php shells
- M3.5 API moved INTO repo (api/) served at /pos/api/; .gitignore added; pushed to GitHub
- M3 task 1: passwords hashed (api/hash_user.php seeder) — keeps DB hashes valid on every machine
- M3 task 2: api/login.php WRITTEN + VALIDATED (admin/password123 -> {"success":true,...})
- M3 task 3: api/logout.php written + verified (HTTP 302 -> /pos/index.html)

M3 — REAL LOGIN (in progress at task 4)
- [ ] 4. index.html: remove the role dropdown (role comes from DB now) — remove the <!-- Role --> div block
- [ ] 5. Rewrite js/login.js: POST JSON to /pos/api/login.php (read key `success`),
         then window.location.href = 'pages/' + data.role + '-dashboard.php'
- [ ] 6. Add session guard + echo $_SESSION at the @M3 markers in all three pages/*.php
         guard: session_start(); if (!isset($_SESSION['user_id'])) { header('Location: ../index.html'); exit; }
         echoes: $_SESSION['full_name'], $_SESSION['role'] (login.php stores them)
         Logout link -> http://localhost/pos/api/logout.php
- [ ] 7. Delete stale pages/*.html (after login routes to .php work)  [AI with user's OK]
- [ ] 8. Test admin/password123 -> Admin dashboard -> logout -> bounced to index.html
- [ ] 9. Teach-back: explain login.php + one guard line-by-line

NEXT SESSION FLOW (contract): micro-drills if needed -> recall login.php from memory ->
finish tasks 4-9 -> commit yourself -> NOTES.md teach-back.

REMINDERS
- PowerShell API test: JSON must be in a FILE, not inline (quotes get stripped).
  curl.exe -s -X POST -H "Content-Type: application/json" -d @body.json http://localhost/pos/api/login.php
- api files live in repo -> http://localhost/pos/api/...
- login.php reply key is `success`; $_SESSION keys: user_id, username, full_name, role

M4+ (later)
- Checkout/charge: subtotal->discount->tax->total, save transaction to DB
- Inventory restock, reports/dashboard stats, payroll (per the Excel checklist)